name: opentyphoon
description: A Dart package for integrating OpenTyphoon AI's large language models (LLM) into your applications.
version: 1.0.0
repository: https://github.com/anoochit/opentyphoon

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  http: ^1.2.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
